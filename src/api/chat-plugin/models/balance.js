/**
 * @class
 * @name Balance
 */
class Balance {

  constructor(database) {
    this.database = database;
  }

  /**
   * @param {String} accountNumber
   */
  query({ accountNumber }) {
  }

}

module.exports = Balance;
